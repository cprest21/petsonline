<%- model_class = Advertisement -%>
<div class="page-header">
  <h1><%=t '.title', :default => model_class.model_name.human.titleize %></h1>
</div>

<dl class="dl-horizontal">
  <dt><strong><%= model_class.human_attribute_name(:user_id) %>:</strong></dt>
  <dd><%= @advertisement.user_id %>
    <%# u = User.where(:id => @advertisement.user_id).first %>
    <%# if u.role == "owner" %>
        <%# @o = Owner.where(:user_id => u.id) %>
        <%#= @o.name %><!--&nbsp;--><%#= @o.lastname %> 
    <%# elsif u.role == "advertiser" %>
        <%# @a = Advertiser.where(:user_id => u.id).first %>
        <%#= @a.name %><!--&nbsp;--><%#= @a.lastname %>
    <%# end %>
  </dd> 
  <dt><strong><%= model_class.human_attribute_name(:details) %>:</strong></dt>
  <dd><%= @advertisement.details %></dd>
  <dt><strong><%= model_class.human_attribute_name(:placement_date) %>:</strong></dt>
  <dd><%= @advertisement.updated_at %></dd>
</dl>

<div class="form-actions">
  <%= link_to t('.back', :default => t("helpers.links.back")),
              advertisements_path, :class => 'btn'  %>
  <%= link_to t('.edit', :default => t("helpers.links.edit")),
              edit_advertisement_path(@advertisement), :class => 'btn' %>
  <%= link_to t('.destroy', :default => t("helpers.links.destroy")),
              advertisement_path(@advertisement),
              :method => 'delete',
              :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) },
              :class => 'btn btn-danger' %>
</div>
