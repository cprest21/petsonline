<%= simple_form_for @veterinarian, :html => { :class => 'form-horizontal' } do |f| %>
  <%= f.input :address_id %>
  <%= f.input :name %>
  <%= f.input :lastname %>
  <div class="form-actions">
    <%= f.button :submit, :class => 'btn-primary' %>
    <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                veterinarians_path, :class => 'btn' %>
  </div>
<% end %>
